spring.application.name=leavebooking
spring.jpa.hibernate.ddl-auto=none
spring.profiles.active=local
# Do not expose the underlying web server/version in HTTP responses.
server.server-header=LeaveBookingAPI